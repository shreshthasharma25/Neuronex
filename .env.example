# NeuroNex Environment Variables
# Copy to .env or set in your environment

# Google Gemini API Key (Never exposed to browser code - used only by backend /api/chat)
GEMINI_API_KEY=

# Optional: Supabase configuration (if cloud persistence is desired)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
